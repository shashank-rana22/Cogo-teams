import TaskView from './TaskView';

// const SOURCE = ['consol', 'coload'];

function Tasks() {
	// return SOURCE.includes(shipment_data.source) ? <div>Consol / Coload</div> : <TaskView />;
	return <TaskView />;
}

export default Tasks;
